# Streamlit Secrets Template
# Copy this to secrets.toml for local testing, or add to Streamlit Cloud secrets

# Password for accessing the report
password = "your-secure-password-here"

# Google OAuth credentials (copy from your token.pickle contents)
# To get these values, run this Python code locally after authenticating:
#
# import pickle
# with open('token.pickle', 'rb') as f:
#     creds = pickle.load(f)
# print(f'token = "{creds.token}"')
# print(f'refresh_token = "{creds.refresh_token}"')
# print(f'client_id = "{creds.client_id}"')
# print(f'client_secret = "{creds.client_secret}"')

[google_oauth]
token = "your-access-token"
refresh_token = "your-refresh-token"
token_uri = "https://oauth2.googleapis.com/token"
client_id = "your-client-id.apps.googleusercontent.com"
client_secret = "your-client-secret"

